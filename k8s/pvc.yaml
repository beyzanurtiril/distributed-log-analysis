apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: log-data-pvc
  namespace: log-analysis
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
